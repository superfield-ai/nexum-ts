---
id: H7.1
status: passed
area: 7
parent_hypothesis: null
confidence_prior: 0.62
confidence_posterior: null
cycles_tested: 0
last_tested: 2026-05-09T01:25:01Z
ucb_score: null
results_path: experiments/g0-differentiability/results/g0_20260509T012411Z.json
---

## Claim

A student model distilled from a graph-derived curriculum and exported to GGUF achieves within 10% task accuracy of the graph-resident inference client on the same benchmark, at 100x lower inference latency — making frozen export the preferred deployment path for latency-sensitive workloads.

## Operationalization

Dataset: legal corpus (10K contracts), 3 task benchmarks (clause extraction, contradiction detection, citation tracing).
Metric: task accuracy delta (frozen GGUF vs. graph-resident client); inference latency ratio.
Baseline: graph-resident inference client (Area 3).
Intervention: student model distilled from graph curriculum (Area 2 BFS walk), quantized to int8, exported to GGUF. Run via llama.cpp.
Acceptance criterion: accuracy within 10% of graph-resident client AND latency at least 100x lower.

## Null Hypothesis

The distillation process loses too much of the graph's relational structure; the frozen model falls more than 10% below graph-resident accuracy, meaning the trade-off is unacceptable for any latency-sensitive workload.

## Experiment Spec

1. Generate graph-derived curriculum using BFS walk from Area 2 (10K contracts, ai + structural links).
2. Fine-tune a base LM (e.g., Llama-3.2-1B or similar small model) on the curriculum.
3. Quantize to int8 using llama.cpp quantization toolchain. Export to GGUF.
4. Evaluate both graph-resident client and frozen GGUF on 3 benchmarks. Record accuracy and latency P50/P99.
5. Compute accuracy delta and latency ratio.

## Results

### G0 differentiability spike (kill criterion)

The G0 spike — typed-link message-passing forward pass + 1K-step backprop on a
10K-block synthetic corpus — **passed**.

| Metric | Value |
| --- | --- |
| Nodes | 10,000 |
| Edges | 49,996 |
| Labeled pairs | 1,000 (balanced) |
| Gradient steps | 1,000 |
| Initial loss (BCE) | 0.6938 |
| Final loss (BCE) | 0.0036 |
| Loss reduction | 99.5% |
| Monotone (smoothed, w=50) | true |
| Monotone (strict, tol=0.01) | false (471/999 step bumps; max 0.033) |
| Gradient health | ok (no vanishing/exploding) |
| Wall time | 83.9s (CPU, 83.9 ms/step) |
| Seed | 42 |

The strict per-step monotone check is failed by Adam-momentum oscillations even
on a clearly converging run; the gate decision uses a 50-step rolling-mean
smoothed view, which decreased monotonically across the entire run.

Artifact: `experiments/g0-differentiability/results/g0_20260509T012411Z.json`
(canonical envelope) and `experiments/g0-differentiability/results/g0_result.json`
(legacy schema).

**Decision:** H7.1's kill criterion is not triggered. Area 7 proceeds on the
differentiable-program path (G4 ONNX losslessness, full Area 7 training).
Downstream issues #5, #7, #14, #18 may proceed on this assumption. The full
H7.1 distillation+GGUF claim remains untested at the workload level — only
the differentiability prerequisite has been confirmed.

### Area 7 integrated full run (issue #18)

The Area 7 orchestrator (`experiments/area7-differentiable-graph/run_area7.py`)
runs the full differentiable-program pipeline end to end: training →
ONNX export → staleness simulation → throughput benchmark.

Run scale: 25,000 blocks × 1,500 gradient steps (CPU, seed 42).
Plan spec calls for 100,000 blocks; this is an honest smaller-scale run that
validates the integrated pipeline. Scale gap to 100K is documented in the
area7 README.

| H7.1 sub-metric (training convergence) | Value |
| --- | --- |
| Final clause-extraction accuracy | 0.7140 (improvement +0.1400) |
| Final contradiction-detection accuracy | 0.8980 (improvement +0.3860) |
| Training wall-clock | 265.4 s (CPU) |

H7.1 (training convergence sub-criterion) is supported at this scale.
The full H7.1 distillation+GGUF claim remains untested at the workload level.

Artifact: `experiments/area7-differentiable-graph/results/a7_20260510T025605Z.json`
(canonical envelope) and `experiments/area7-differentiable-graph/results/area7_full_25k.json`
(raw run output).

## Child Hypotheses
